# 本工具改为纯 Python 标准库实现（urllib / http.server），无需任何第三方依赖。
#
# 原因：本机 requests/urllib3（google-* 库底层用它）连 Google HTTPS 必崩
# （SSL: UNEXPECTED_EOF_WHILE_READING）；标准库 urllib.request 连 Google 正常。
# 所以 oauth_setup.py / post_comment.py 全部改用 urllib，不再依赖
# google-api-python-client / google-auth-oauthlib / google-auth-httplib2 / requests。
#
# 无需 pip install。
